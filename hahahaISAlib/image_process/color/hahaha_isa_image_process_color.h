//---------------------------------------------------------------------------

#ifndef hahaha_isa_image_process_colorH
#define hahaha_isa_image_process_colorH
//---------------------------------------------------------------------------
#pragma once
//---------------------------------------------------------------------------
#include <cstdint>
#include <immintrin.h>
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
namespace hahahaISAlib
{
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
namespace image_process
{
//---------------------------------------------------------------------------
// From Microsoft Copilot
//---------------------------------------------------------------------------
namespace color
{
//---------------------------------------------------------------------------
class hahaha_isa_image_process_color
{
public:
	static void Convert(
        const uint8_t* src,
        int src_stride,
        uint8_t* dst,
        int dst_stride,
        int width,
        int height
	);

	static bool hasAVX2();

    // ---------------- Scalar（正確基準版） ----------------
    static void Convert_Scalar(
        const uint8_t* src,
        int src_stride,
        uint8_t* dst,
        int dst_stride,
        int width,
        int height
	);
#ifdef __AVX2__
    // ---------------- AVX2（與 Scalar 顏色對齊的版本） ----------------
    static void Convert_AVX2(
        const uint8_t* src,
        int src_stride,
        uint8_t* dst,
        int dst_stride,
        int width,
        int height
	);

    // 8 pixels，32-bit 精準版，BGRA（與 Scalar 對齊）
    static inline void process8(
        uint8_t* dst,
        __m256i y16,
        __m256i u16,
        __m256i v16
	);

public:
    // ---------------- 驗證：AVX2 與 Scalar 是否完全一致 ----------------
	static void Verify(const uint8_t* src,
		int src_stride,
		int width,
		int height
	);
#endif

public:
public:
};


//---------------------------------------------------------------------------
} // color
//---------------------------------------------------------------------------

//---------------------------------------------------------------------------
} // image_process
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
} // hahahaISAlib
//---------------------------------------------------------------------------

//---------------------------------------------------------------------------
namespace haISAlib_image
{
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------

typedef hahahaISAlib::image_process::color::hahaha_isa_image_process_color color;



//---------------------------------------------------------------------------

//---------------------------------------------------------------------------
} // halib_image
//---------------------------------------------------------------------------

#endif
